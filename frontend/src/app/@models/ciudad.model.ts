export interface Ciudad {
  id: number;
  descripcion: string;
  id_provincia: number; // Esta propiedad representa la relación con Provincia
  // Puedes agregar más propiedades según sea necesario
}
